<%= form_for(@experiment_detail) do |f| %>
  <% if @experiment_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@experiment_detail.errors.count, "error") %> prohibited this experiment from being saved:</h2>

      <ul>
      <% @experiment_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	 <div class="field">
    <%= f.label :project_id %><br>
    <%= f.select :project_id, options_for_select(Project.all.map{|s|[s.title, s.id]}) %>
  </div>

  
 
  <div class="field">
    <%= f.label :sample_id %><br>
    <%= f.select :sample_id, options_for_select(Sample.all.map{|s|[s.name, s.id]}) %>

  </div>
  <div class="field">
    <%= f.label :platform_id %><br>
           <%= f.select :platform_id, options_for_select(Platform.all.map{|s|[s.name, s.id]}) %>

  </div>
  <div class="field">
    <%= f.label :protocol_id %><br>
    <%= f.select :protocol_id, options_for_select(Protocol.all.map{|s|[s.name, s.id]}) %>

  </div>
  <div class="field">
    <%= f.label :disease_id %><br>
    <%= f.select :disease_id, options_for_select(Disease.all.map{|s|[s.name, s.id]}) %>

  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name ,:class=>"input-xxlarge" %>
  </div>
 <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description ,:class=>"input-xxlarge",:rows => 6 %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment ,:class=>"input-xxlarge",:rows => 5%>
  </div>
  <div class="actions">
    <%= f.submit "Create Experiment" %>
  </div>
<% end %>

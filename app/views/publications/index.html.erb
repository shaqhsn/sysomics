
<h1>Listing publications</h1>

<table class="table table-striped table-bordered table-condensed">  <thead>
    <tr>
      <th>Project</th>
      <th>Public repository</th>
      <th>Doi</th>
       <th>URL</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @publications.each do |publication| %>
      <tr>
        <td><%= publication.project.title %></td>
        <td><%= publication.public_repository_id %></td>
        <td><%= publication.DOI %></td>
        <td><%= link_to publication.url ,publication.url%></td>
        <td><%= link_to 'Show', publication %></td>
        <td><%= link_to 'Edit', edit_publication_path(publication) %></td>
        <td><%= link_to 'Destroy', publication, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Publication', new_publication_path %>

